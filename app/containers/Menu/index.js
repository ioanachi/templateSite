import * as React from 'react';
import MenuTabs from './MenuTabs';
import MenuFilters from './MenuFilters';

const Menu = () => (
  <div>
    <MenuTabs />
    <MenuFilters />
  </div>
);

export default Menu;
